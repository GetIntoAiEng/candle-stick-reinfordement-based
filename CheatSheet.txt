
## Git Stuff ##################################################################

-- Check State ----------------------------------------------------------------

    git status

-- Add Changed Code -----------------------------------------------------------

    git add .
    git commit -m "why would I commit"
    git push

-- Add a new file -------------------------------------------------------------

    git add file-path/new-file-name
    git commit -m "why would I commit"
    git push    

-- rename a file --------------------------------------------------------------  

    git mv <oldname> <newname>
    git commit -m "why would I commit"
    git push    

-- jump back to branch --------------------------------------------------------

    git checkout -- <this_file>     # for one file  
    git restore .                   # for them all

++ B R A N C H E S ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

-- Which Branches we got and were are   we ------------------------------------

    git branch
    or
    git status

-- Change Branch to -----------------------------------------------------------

    git switch <branch-name>

-- Create A New Branch --------------------------------------------------------

    git checkout -b <prefix>/<new-branch-name>
    git push -u origin <new-branch-name>
    git branch  

    with prefix:
        - feature/      → For new features (e.g., feature/data-exploration).
        - bugfix/       → For fixing bugs (e.g., bugfix/fix-reward-calculation).
        - hotfix/       → For urgent fixes in production.
        - release/      → For preparing a release version.
        - experiment/   → For trying out ideas without merging into main.

-- merge back 2 main ----------------------------------------------------------

    git checkout main
    git pull
    git merge <your-current-branch-name>

++ Fetching stuff +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

-- get an existing Repro ------------------------------------------------------

    git clone https://github.com/GetIntoAiEng/candle-stick-reinfordement-based.git
    cd candle-stick-reinfordement-based

-- list all branch ------------------------------------------------------------

    git branch -r       (r = remote)


-- checkout an branch ---------------------------------------------------------
    git fetch
    git checkout -b branch-name origin/branch-name

-- update a branch from an other locations change -----------------------------
    git fetch origin
    git checkout <branch-name>
    git pull origin <branch-name>



## GitHub Stuff ###############################################################

-- switch enviroment to python 3.11 -------------------------------------------
    python3 -m venv .venv
    source .venv/bin/activate 

